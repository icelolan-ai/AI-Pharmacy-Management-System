# Phase 5 Gate — รายการตรวจก่อนปิดเฟส

เอกสารนี้มี 2 ส่วน

1. **รายการทดสอบด้วยมือ** — ผู้ใช้ทำเองหน้าจอจริง เขียนเป็นขั้นตอนสั้น ๆ ว่าต้องกดอะไร และผลที่ถูกต้องคืออะไร
2. **สรุป Phase 5** — ทำอะไรไปบ้าง, commit, เทสต์ทั้งหมด, และของที่ยังไม่ได้ทำ

---

## ส่วนที่ 1 — รายการทดสอบด้วยมือ

### เตรียมตัวก่อนเริ่ม

| สิ่งที่ต้องมี | รายละเอียด |
|---|---|
| หน้าเว็บ | `http://localhost:3000` |
| บัญชี | อย่างน้อย 2 บัญชี **คนละ role** เช่น `owner` 1 คน กับ `staff` 1 คน |
| ข้อมูลร้าน | กรอกไว้แล้วที่หน้า “ข้อมูลร้าน” |
| ยาและผู้จำหน่าย | มีอย่างน้อยอย่างละ 1 รายการ |

**วิธีใช้ตาราง:** ทำตามช่อง “ต้องกดอะไร” ทีละบรรทัด แล้วเทียบกับช่อง “ผลที่ถูกต้อง”
ถ้าตรง = ผ่าน, ถ้าไม่ตรง = จดไว้ว่าหน้าไหน ข้อไหน และเห็นอะไรแทน

> ระหว่างทดสอบ ถ้าเจอข้อความ **“ยังไม่มีการตัดสต็อก”** หรือ **“ยังไม่มีการรับเข้าคลัง”**
> แปลว่าระบบบันทึกไม่สำเร็จ **และไม่ได้แตะสต็อกเลย** — ปลอดภัย กดใหม่ได้

---

### T1 — เข้าระบบ ออกจากระบบ และสลับผู้ใช้คนละ role 🔴 ข้อสำคัญที่สุด

ข้อนี้ตรวจว่าชื่อและสิทธิ์ของคนเก่าไม่หลงค้างมาให้คนใหม่เห็น

| # | ต้องกดอะไร | ผลที่ถูกต้อง |
|---|---|---|
| 1 | เปิด `http://localhost:3000` แล้ว login ด้วยบัญชีแรก | เข้าหน้าแรกได้ · **มุมขวาบน**ขึ้นชื่อและตำแหน่งของบัญชีแรก · เมนูซ้ายขึ้นครบตามตำแหน่งนั้น |
| 2 | กด **F5** (refresh หน้าเว็บ) | ชื่อและตำแหน่งเดิมกลับมาเหมือนเดิม · **ต้องไม่ค้าง**ที่คำว่า “กำลังโหลดสิทธิ์...” · ไม่เด้งออกไปหน้า login |
| 3 | กดปุ่ม **ออกจากระบบ** (มุมขวาบน) | เด้งไปหน้า login ทันที · ชื่อผู้ใช้หายไปจากมุมขวาบน |
| 4 | login ด้วยบัญชีที่สอง (**คนละ role**) | ชื่อและตำแหน่งเป็นของ**คนใหม่ทันที** · เมนูซ้ายเปลี่ยนตามสิทธิ์ของคนใหม่ · **ต้องไม่เห็นชื่อหรือเมนูของคนเก่าแม้แวบเดียว** |
| 5 | ระหว่างข้อ 4 ให้จ้องมุมขวาบนตอนกด login | ถ้าเห็นชื่อคนเก่ากระพริบขึ้นมาก่อน = **ไม่ผ่าน** ให้จดไว้ |

---

### T2 — ข้อมูลร้าน

| # | ต้องกดอะไร | ผลที่ถูกต้อง |
|---|---|---|
| 1 | เมนูซ้าย → **ข้อมูลร้าน** | เห็นข้อมูลที่กรอกไว้ครบ |
| 2 | แก้ชื่อร้านเป็นชื่ออื่นชั่วคราว → **บันทึก** | ขึ้นว่าบันทึกสำเร็จ |
| 3 | ดู**มุมบนซ้าย**ของเมนู | ชื่อร้านเปลี่ยนตามทันที |
| 4 | แก้ชื่อกลับเป็นของเดิม → **บันทึก** | กลับมาเหมือนเดิมทั้งสองที่ |
| 5 | login เป็น **staff** แล้วเข้าเมนูข้อมูลร้าน | **ไม่มีเมนูนี้** ให้เห็น (ถ้าพิมพ์ URL เข้าตรง ๆ ต้องขึ้นว่าไม่มีสิทธิ์) |

---

### T3 — ยาและผู้จำหน่าย

| # | ต้องกดอะไร | ผลที่ถูกต้อง |
|---|---|---|
| 1 | เมนู → **คลังยา** → ปุ่มเพิ่มยา | กล่องกรอกเปิดขึ้นมา **ว่างเปล่าทุกช่อง** |
| 2 | กรอกชื่อยา หน่วย ราคาขาย → บันทึก | ยาใหม่โผล่ในตาราง |
| 3 | กดปุ่มเพิ่มยา**อีกครั้ง** | กล่องต้อง**ว่างเปล่าอีกครั้ง** ไม่มีค่าเก่าค้าง 🔴 |
| 4 | กดที่ชื่อยาในตาราง → ปุ่มแก้ไข | กล่องเปิดมาพร้อมข้อมูลยาตัวนั้น (ไม่ใช่ของตัวอื่น) |
| 5 | ปิดกล่องโดยไม่บันทึก แล้วเปิดใหม่ | ข้อมูลกลับมาเป็นของยาตัวนั้นเหมือนเดิม ไม่มีค่าที่พิมพ์ค้างไว้ |
| 6 | ทำข้อ 1–5 ซ้ำที่เมนู **ผู้จำหน่าย** | ผลเหมือนกันทุกข้อ |

---

### T4 — รับสินค้าเข้าคลัง (2 ขั้นตอน)

| # | ต้องกดอะไร | ผลที่ถูกต้อง |
|---|---|---|
| 1 | เมนู → **รับสินค้า** → สร้างใบใหม่ | เข้าหน้า “รับสินค้า — ขั้นที่ 1 ตามใบส่งของ” |
| 2 | เลือกผู้จำหน่าย, กรอกเลขที่ใบส่งของ, เพิ่มยา 1 รายการ พร้อมจำนวน ราคาทุน เลขล็อต วันหมดอายุ | กรอกได้ครบ · มุมจอบอกสถานะว่ากำลังบันทึกร่าง / บันทึกแล้ว |
| 3 | **รอ 3 วินาที** แล้วกด F5 | ของที่กรอกไว้**ยังอยู่ครบ** (ระบบบันทึกร่างให้อัตโนมัติ) 🔴 |
| 4 | กดไป **ขั้นที่ 2 ตรวจสอบ** | เห็นสรุปรายการตามที่กรอก |
| 5 | กดยืนยันรับสินค้า | ขึ้นว่า “รับสินค้าเรียบร้อย” · ได้**เลขที่ใบรับ** รูปแบบ `R-ปีเดือนวัน-ลำดับ` |
| 6 | เมนู → **คลังยา** → หายาตัวนั้น | จำนวนคงเหลือเพิ่มขึ้นตามที่รับเข้า |
| 7 | ลองสร้างใบใหม่แล้ว**ทิ้งไว้ไม่ยืนยัน** จากนั้นสร้างอีกใบและยืนยัน | เลขที่ใบรับของใบที่ยืนยัน **ต้องไม่ข้ามเลข** (ใบที่ทิ้งไม่กินเลข) |

---

### T5 — ขายยา

| # | ต้องกดอะไร | ผลที่ถูกต้อง |
|---|---|---|
| 1 | เมนู → **ขายยา** | เคอร์เซอร์อยู่ที่ช่องค้นหา · หน้าจอเขียนว่า “กด F2 หรือ Ctrl+Enter” |
| 2 | พิมพ์ชื่อยา | รายการขึ้นมาให้เลือก · ถ้า**ลบข้อความจนว่าง** รายการต้องหายไปหมด |
| 3 | เลือกยา ใส่จำนวน | ยาเข้าตะกร้า · มียอดรวมโดยประมาณ |
| 4 | กด **F2** | กล่องยืนยันการขายเปิดขึ้น |
| 5 | **รีบกดปุ่มยืนยันทันที** ภายในเสี้ยววินาทีแรก | ปุ่มต้อง**กดไม่ได้**ในช่วงสั้น ๆ แรก (กันกดพลาด) แล้วจึงกดได้ 🔴 |
| 6 | กดยืนยัน | ขายสำเร็จ · ได้**เลขที่บิล** รูปแบบ `S-ปีเดือนวัน-ลำดับ` |
| 7 | ขายอีกครั้ง แล้วตอนกล่องยืนยันเปิด ให้**กดปุ่มยืนยันรัว ๆ หลายครั้ง** | ได้**บิลใบเดียว** ไม่ใช่หลายใบ 🔴 |
| 8 | กด **Ctrl+Enter** แทน F2 | ได้ผลเหมือน F2 |
| 9 | กดปุ่ม **/** (สแลช) | เคอร์เซอร์กระโดดกลับไปช่องค้นหา |
| 10 | เมนู → **คลังยา** | จำนวนยาลดลงตามที่ขาย |

---

### T6 — ปรับจำนวนสต็อก และการชนกัน (D23) 🔴

ข้อ 4–7 คือหัวใจ: ถ้ามีคนขายยาตัวเดียวกันระหว่างที่เราเปิดหน้าต่างปรับอยู่
ระบบต้องไม่ยอมบันทึกทับ **และต้องไม่ลบสิ่งที่เราพิมพ์ไปแล้ว**

| # | ต้องกดอะไร | ผลที่ถูกต้อง |
|---|---|---|
| 1 | **คลังยา** → กดชื่อยา → เลือกล็อต → ปุ่มปรับจำนวน | กล่องเปิดมา แสดง “จำนวนในระบบตอนนี้” ตรงกับที่เห็นในตาราง |
| 2 | ใส่จำนวนที่นับได้ เลือกเหตุผล กดบันทึก | บันทึกสำเร็จ · จำนวนในตารางเปลี่ยนตาม |
| 3 | เลือกเหตุผล **“อื่น ๆ”** แล้วเว้นช่องรายละเอียดว่าง | ปุ่มบันทึก**กดไม่ได้** · มีข้อความบอกว่าต้องกรอกรายละเอียด |
| 4 | เปิด**เบราว์เซอร์อีกหน้าต่าง** ไปหน้าขายยา แล้วขายยาตัวเดียวกันออกไป 1 ชิ้น | ขายสำเร็จ |
| 5 | กลับมาหน้าต่างแรก (กล่องปรับจำนวนยังเปิดค้างอยู่) พิมพ์จำนวนที่นับได้ เลือกเหตุผล พิมพ์รายละเอียด แล้วกดบันทึก | ขึ้นข้อความ **“จำนวนคงเหลือเปลี่ยนไประหว่างที่เปิดหน้าต่างนี้ กรุณาตรวจนับใหม่”** |
| 6 | ดูในกล่องที่ยังเปิดอยู่ | **จำนวนที่พิมพ์ · เหตุผลที่เลือก · รายละเอียดที่พิมพ์ ต้องยังอยู่ครบทั้ง 3 อย่าง** 🔴 · และ “จำนวนในระบบตอนนี้” เปลี่ยนเป็นเลขจริงล่าสุดแล้ว |
| 7 | กดบันทึกซ้ำอีกครั้งโดย**ไม่ต้องพิมพ์อะไรใหม่** | บันทึกสำเร็จ · ผลต่างคิดจากเลขจริงล่าสุด |

---

### T7 — รายงานและสิทธิ์การเห็นต้นทุน

ทำ **2 รอบ**: รอบแรกเป็น owner รอบสองเป็น staff

| # | ต้องกดอะไร | ผลที่ถูกต้อง (owner) | ผลที่ถูกต้อง (staff) |
|---|---|---|---|
| 1 | เมนู → **ภาพรวมร้าน** | เห็นตัวเลขสรุปครบ | ไม่มีเมนูนี้ |
| 2 | เมนู → **ยาใกล้หมด** | เห็นรายการ | ไม่มีเมนูนี้ |
| 3 | เมนู → **ใกล้หมดอายุ** | เห็นรายการ แบ่งตามระดับความเสี่ยง | ไม่มีเมนูนี้ |
| 4 | เมนู → **หมดอายุแล้ว** | เห็นรายการ | ไม่มีเมนูนี้ |
| 5 | เมนู → **มูลค่าคลังยา** | เห็นมูลค่ารวม | **ไม่มีเมนูนี้** (owner เท่านั้น) |
| 6 | เมนู → **ตรวจสอบย้อนหลัง** | เห็นรายการ | **ไม่มีเมนูนี้** (owner เท่านั้น) |
| 7 | staff พิมพ์ URL ของหน้าเหล่านั้นเข้าตรง ๆ | — | ต้องขึ้นว่า**ไม่มีสิทธิ์** ไม่ใช่แสดงข้อมูล 🔴 |

---

### T8 — ประวัติและการตรวจย้อนหลัง

ทำในฐานะ **owner** ยกเว้นข้อ 5–6

| # | ต้องกดอะไร | ผลที่ถูกต้อง |
|---|---|---|
| 1 | เมนู → **ประวัติการขาย** | เห็นบิลที่เพิ่งขายใน T5 · มีเลขที่บิลและชื่อคนขาย |
| 2 | กดเข้าไปดูบิลนั้น | เห็นรายการยา จำนวน ราคา ตรงกับที่ขาย |
| 3 | เมนู → **ประวัติรับสินค้า** | เห็นใบรับจาก T4 · มีเลขที่ใบรับ ชื่อคนรับ และคอลัมน์มูลค่า/ต้นทุน |
| 4 | เมนู → **ตรวจสอบย้อนหลัง** | เห็นรายการทุกอย่างที่ทำไปใน T2–T6 พร้อมชื่อคนทำและเวลา |
| 5 | กดปุ่มไปหน้าถัดไปของรายการ | เปลี่ยนหน้าได้ ข้อมูลไม่ซ้ำกับหน้าก่อน |
| 6 | login เป็น **staff** แล้วดูเมนูซ้าย | **ไม่มีเมนูประวัติทั้งสองอัน และไม่มีตรวจสอบย้อนหลัง** |
| 7 | staff พิมพ์ URL `/history/purchases` เข้าตรง ๆ | ขึ้นว่าไม่มีสิทธิ์ 🔴 |

---

### T9 — พิมพ์เอกสาร

| # | ต้องกดอะไร | ผลที่ถูกต้อง |
|---|---|---|
| 1 | เปิดบิลขายจาก T8 → ปุ่มพิมพ์ | ตัวอย่างใบเสร็จขึ้นมา ขนาด **A5** |
| 2 | ดูหัวใบเสร็จ | ชื่อร้าน ที่อยู่ เบอร์โทร **ตรงกับที่กรอกในหน้าข้อมูลร้านตอนนี้** |
| 3 | ดูทั้งใบเสร็จให้ทั่ว | **ต้องไม่มีเลขประจำตัวผู้เสียภาษีปรากฏ** 🔴 |
| 4 | เปลี่ยนชื่อร้านในหน้าข้อมูลร้าน แล้วเปิดใบเสร็จ**ใบเดิม**อีกครั้ง | ชื่อร้านบนใบเสร็จ**เปลี่ยนตามค่าล่าสุด** (D21) |
| 5 | เปลี่ยนชื่อร้านกลับเป็นของเดิม | — |
| 6 | เปิดใบรับสินค้าจาก T8 → ปุ่มพิมพ์ | ใบรับขนาด **A4** · มีเลขที่ใบรับ ชื่อผู้จำหน่าย เลขที่ใบส่งของ ชื่อคนรับ และวันที่ยืนยัน |

---

### T10 — สิ่งที่ staff ต้องทำไม่ได้

| # | ต้องกดอะไร (login เป็น staff) | ผลที่ถูกต้อง |
|---|---|---|
| 1 | ดูเมนูซ้ายทั้งหมด | เห็นเฉพาะ **4 อัน**: หน้าแรก, ขายยา, คลังยา, ข้อมูลของฉัน |
| 2 | เข้า **คลังยา** → ดูตาราง | **ไม่เห็นราคาทุน** · ไม่มีปุ่มเพิ่ม/แก้ไขยา |
| 3 | เข้าหน้ารายละเอียดยา | **ไม่มีปุ่มปรับจำนวน** |
| 4 | ขายยา | ทำได้ตามปกติ · **แก้ราคาขายไม่ได้** (ใช้ราคาที่ตั้งไว้เท่านั้น) 🔴 |
| 5 | พิมพ์ URL `/receiving` เข้าตรง ๆ | ขึ้นว่าไม่มีสิทธิ์ |

---

### ผลการทดสอบ — รอบที่ 1 (23 ก.ย. 2569)

ทดสอบบนฐานข้อมูล **prod** · owner = `ice boonak` · staff = `พนักงานทดสอบ`

| ข้อ | ผล | หมายเหตุ |
|---|---|---|
| T1 | ✅ ผ่าน | ข้อ 5 (จ้องว่าชื่อคนเก่ากระพริบไหม) ผู้ใช้สลับบัญชีเอง ยังไม่มีใครยืนยันจุดนี้ |
| T2 | ✅ ผ่าน | staff ไม่เห็นเมนูข้อมูลร้าน และพิมพ์ URL เข้าตรง ๆ ถูกปฏิเสธ |
| T3 | ✅ ผ่าน | กล่องเพิ่มยาเปิดมาว่างทุกครั้ง · พิมพ์ทิ้งแล้วปิด เปิดใหม่ค่าหาย |
| T4 | ✅ ผ่าน | ร่างรอดหลัง F5 · `R-690923-001` · ใบร่างที่ทิ้งไม่กินเลข → `R-690923-002` |
| T5 | ✅ ผ่าน | กดรัว 5 ครั้งได้บิลเดียว · ปุ่มตาย 300 ms แรก · F2 · Ctrl+Enter · `/` |
| T6 | ✅ ผ่าน | **D23 ชนกันจริง 2 แท็บ: 409 แล้วค่าที่พิมพ์อยู่ครบทั้ง 3 ช่อง** |
| T7 | ✅ ผ่าน | owner เห็นครบ 6 หน้า · staff ถูกปฏิเสธทุกหน้า ไม่มีข้อมูลรั่ว |
| T8 | ✅ ผ่าน | เคยไม่ผ่านข้อ 3 (บั๊ก list endpoint) แก้แล้วใน `ce7e88b` |
| T9 | ✅ ผ่าน | ใบเสร็จ A5 ไม่มีเลขผู้เสียภาษี · เปลี่ยนชื่อร้านแล้วใบเดิมเปลี่ยนตาม (D21) |
| T10 | ✅ ผ่าน | เมนูเหลือ 4 อัน · ไม่เห็นต้นทุน · ไม่มีปุ่มปรับ · แก้ราคาขายไม่ได้ |

**หลักฐานสำคัญที่ได้จากรอบนี้**

- **D32/D33 ทำงานจริง** — staff พิมพ์ URL เข้าตรง ๆ ทั้ง 11 หน้าที่ถูกกั้น
  ได้ `🔒 หน้านี้สำหรับเจ้าของร้านเท่านั้น` ทุกหน้า และ **ไม่มีตัวเลขเงินหรือแถวข้อมูลหลุดออกมาแม้แถวเดียว**
- **ตัวกรองกลุ่ม 2 ที่ D33 ห้ามลบ ทำงานจริง** — หน้าคลังยาของ staff
  คอลัมน์ “มูลค่า” หายไป · หน้ารายละเอียดยาไม่มีคอลัมน์ “ต้นทุน/หน่วย” และไม่มีการ์ดมูลค่า
- **D11 ทำงานจริง** — หน้าขายของ staff ทั้งหน้ามีช่องกรอกแค่ 2 ช่อง
  (ค้นหา และ จำนวน) ราคาเป็นข้อความอย่างเดียว ไม่มีช่องส่วนลด

**ข้อมูลทดสอบที่สร้างไว้ใน prod**

ยา `TEST-Amoxicillin 500 mg` · ผู้จำหน่าย `TEST-บริษัทยาทดสอบ` ·
ใบรับ `R-690923-001`, `R-690923-002` · ใบร่างค้าง 1 ใบ ·
บิล `S-690923-001`, `S-690923-002` · ปรับสต็อก 1 ครั้ง (18 → 17)

🔴 **ผัง `[ผังตัวอย่าง — ยังไม่ครบ รอผู้ใช้วาดเองใน U-8.4]`** (U-8.3, 24 ก.ย. 2569) — 9 ชิ้น · 0 จุด
สร้างไว้ใน prod เพื่อให้ผู้ใช้ดูผังด้วยตาตาม D41 **ไม่ใช่ผังจริงของร้าน**
ไม่ได้ seed ในโค้ดหรือ migration — เป็นแถวในฐานข้อมูลล้วน ลบได้ด้วยการลบแถวเดียวใน `store_maps`
(จุดและของในห้องหายตามด้วย cascade)

ผังรอบแรกถูกลบทิ้งแล้ว เพราะ**เดาตำแหน่งเอง**ทั้งที่แบบมีเส้นบอกระยะอยู่ — ผิดกฎข้อ 4
รอบนี้ใส่เฉพาะชิ้นที่มีตัวเลขในแบบ และ**จงใจเว้นว่าง** 3 อย่างที่แบบไม่ได้ระบุ:
เชลฟ์กลางร้าน 5 ตัว · เคาน์เตอร์ · ตำแหน่งเสา

**ทั้งหมดในหัวข้อนี้ต้องลบก่อนขึ้น Production**

## ส่วนที่ 2 — สรุป Phase 5

### 2.1 สิ่งที่ทำไปทั้งหมด

| งาน | ได้อะไร |
|---|---|
| 5.1 | โครง Next.js + เข้าสู่ระบบด้วย Supabase + ตัวเรียก API + จัดการ 401 |
| 5.2 | หน้ายาและผู้จำหน่าย · หน่วยนับยา (migration 004) · ข้อมูลร้าน (005) |
| 5.3 | คลังยา · รายละเอียดยาและล็อต · ปรับจำนวนสต็อก + **D23** กันการบันทึกทับ |
| 5.6 | ภาพรวมร้าน + รายงาน 4 หน้า (ยาใกล้หมด, ใกล้หมดอายุ, หมดอายุแล้ว, มูลค่าคลังยา) |
| 5.4 | ขายยาแบบ FEFO · ใบเสร็จ A5 · **D25** · **D26** เลขที่บิล · **D27** ชื่อคนขาย |
| 5.5 | รับสินค้า 2 ขั้นตอน + บันทึกร่างอัตโนมัติ · ใบรับ A4 · **D28/D29/D30** |
| 5.7 | ประวัติการขาย/การรับสินค้า · ตรวจสอบย้อนหลัง · **D32** คุมการเห็นต้นทุน |
| D31 | ชื่อเจ้าของร้าน · เลขผู้เสียภาษีเก็บไว้แต่**ไม่พิมพ์ลงใบเสร็จ**ใน Phase 5 |
| เก็บงาน | แก้ 422 → **400** ทุก endpoint · ล้าง lint จนเหลือ 0 errors · สร้างชุดเทสต์ถาวร |

**กติกาสำคัญที่ตั้งไว้ระหว่างเฟสนี้**

- **D21** — เอกสารที่พิมพ์ใช้ข้อมูลร้าน**ปัจจุบัน**เสมอ ไม่ได้เก็บสำเนาไว้
- **D24** — OpenAPI เป็นต้นทางเดียวของชื่อฟิลด์ ถ้าไม่ตรงให้แก้สเปก ห้ามเปลี่ยนชื่อฟิลด์ใน backend
- **VALIDATION_ERROR → 400 เสมอ** ทุก endpoint ไม่มีข้อยกเว้น
- **ข้อมูลร้านจริงห้าม seed และห้าม commit** ผู้ใช้กรอกเองผ่านหน้าจอ

### 2.2 Commit ของ Phase 5

```
28c3702  5.1  โครงเว็บ + Supabase auth + API client
f964aa5  5.1  แก้สถานะโหลดผู้ใช้
5b9474e  5.1  ล้าง session ค้างก่อนเด้งไป login เมื่อ 401
4e53a9b  5.2  ส่งออก OpenAPI + จำกัด inventory-value ให้ owner (D20)
1fa07eb  5.2  หน้ายาและผู้จำหน่าย
80047f0  5.2  migration 004 หน่วยนับยา, 005 ข้อมูลร้าน
dad6d94  5.2  เพิ่ม docs/05-web-spec.md
11407eb  5.2  แก้ข้อความ D21 + ชื่อร้านบนเมนู + D22
93524b7  5.3  คลังยา รายละเอียดยา ปรับจำนวนสต็อก + D23
032ba04  doc  ปรับชื่อฟิลด์ในสเปกให้ตรง OpenAPI + D24
55e8464  5.6  ภาพรวมร้านและรายงาน 4 หน้า
1802dd6  5.4  ขายยา FEFO + ใบเสร็จ A5 + D25
27c1345  5.4  D26 เลขที่บิล + D27 ชื่อคนขาย
8252712  5.5  ช่องกรอกเดือน/ปีหมดอายุ
4b91ef0  D31  ชื่อเจ้าของร้าน
0ca3e10  5.5  backend D28 D29 D30
a6b6384  5.5  รับสินค้า 2 ขั้นตอน + ใบรับ A4
74b9659  5.7  ประวัติและตรวจสอบย้อนหลัง
d51011c  lint กลุ่ม A — derive loading ใน useSection
6eb3b4f  lint กลุ่ม A — ย้ายหน้าอ่านอย่างเดียวที่เหลือมาใช้ useSection
376194a  lint กลุ่ม D — derive สถานะค้นหาและโหลดบนหน้าขาย/รับสินค้า
355db92  lint กลุ่ม B — เปิด dialog ให้สะอาดด้วยการ mount ไม่ใช่ reset ใน effect
bed1a1e  lint กลุ่ม B — dialog ปรับสต็อก mount ต่อการเปิด โดยไม่กระทบ D23
6ff11b6  lint กลุ่ม C — รักษาคิวบันทึกร่างไว้ครบ พร้อมเลิกเขียน ref ตอน render
d629be5  lint กลุ่ม C — โหลดโปรไฟล์จาก session แทนการเขียนกลับ
3779353  test  คุม idRef ที่ย้ายไปอยู่ใน effect
```

### 2.3 เทสต์ทั้งหมด

**Backend — pytest 398 ตัว ผ่านหมด**

| ชุด | จำนวน | รันอย่างไร |
|---|---|---|
| Unit (ไม่แตะฐานข้อมูล) | 322 ผ่าน | `pytest -m "not db"` — 2 วินาที |
| Integration (ฐานข้อมูล **test** เท่านั้น) | 76 ผ่าน | `pytest -m db` — 2 นาที 3 วินาที |

`tests/db_guard.py` บังคับว่า integration test ต่อได้เฉพาะโปรเจกต์ test เท่านั้น

**Web — ชุดตรวจถาวร 9 ไฟล์ 145 ข้อ ผ่านหมด** (`npm run check`)

| ไฟล์ | ข้อ | คุ้มครองอะไร |
|---|---|---|
| `adjust-409.check.mjs` | 18 | D23 — โดน 409 แล้วค่าที่พิมพ์ต้องไม่หาย |
| `auth-profile.check.mjs` | 26 | โหลดโปรไฟล์ไม่วนซ้ำ และไม่ยิงซ้อนเมื่อ session เปลี่ยน |
| `sell-shortcuts.check.mjs` | 13 | F2/Ctrl+Enter · หน่วง 300 ms · กดรัวได้บิลเดียว · ล้มเหลวแล้วตะกร้าไม่หาย |
| `dialog-reset.check.mjs` | 13 | ทุก dialog เปิดมาต้องสะอาด |
| `stock-detail-error.check.mjs` | 9 | เปิด dialog ไม่สำเร็จ ต้องไม่ทำให้ทั้งหน้าขาว |
| `draft-idref.check.mjs` | 9 | ร่างรับสินค้าบันทึกลงใบที่ถูกต้อง |
| `draft-queue.check.mjs` | 8 | C-2 — แก้ 5 ครั้งระหว่างบันทึก = ยิงจริง 2 ครั้ง ไม่ซ้อนกัน |
| `history-access.check.mjs` | 33 | D32/D33 — ต้นทุนถูกกั้นที่ระดับหน้า และตัวกรองที่ทำงานจริงต้องไม่ถูกลบ |
| `source-rules.check.mjs` | 24 | D35 — ห้ามมี library กราฟใน dependencies หรือดึงผ่าน CDN · D48 — ห้ามมีขนาดตัวอักษรนอก scale (`text-[...]`) ในซอร์ส · 🔴 **6.1 — ไม่มีไฟล์ใดใน `web/src` อ้างถึงคีย์ AI หรือเรียก AI โดยตรง · ไฟล์ env ของเว็บไม่มีตัวแปรคีย์ AI · เว็บไม่ได้ติดตั้ง SDK ของผู้ให้บริการ AI (ห้ามลบตลอดไป)** *(เดิมชื่อ `no-chart-library.check.mjs` เปลี่ยนชื่อ 25 ก.ย. 2569 เพราะชื่อแคบกว่าเนื้อหา)* |

**Backend — เทสต์ถาวรที่ห้ามลบ (เพิ่มในงาน 6.1)**

| ไฟล์ | คุ้มครองอะไร |
|---|---|
| `tests/integration/test_storage_db.py` | 🔴 **bucket `invoice-scans` ต้องไม่ public (ห้ามลบตลอดไป)** — ตรวจทั้ง flag `public` ของ bucket และ policy บน `storage.objects` ที่เปิดให้ `anon`/`public` เข้าถึง เพราะ bucket ที่ “private” แต่มี policy ให้ anon อ่าน คือ public ในอีกชื่อหนึ่ง · รันบนโปรเจกต์ test เท่านั้นตาม db_guard |

**กติกาการเขียนเทสต์ที่ใช้อยู่**

1. เทสต์ที่ปกป้องคำวินิจฉัย ต้องพิสูจน์ด้วยการ**ใส่บั๊กกลับแล้วเห็นมัน FAIL จริง** ทุกครั้ง
2. assertion ทุกตัวต้องถูกทดสอบด้วย**บั๊กที่ตั้งใจให้รอด** ไม่ใช่แค่บั๊กที่ตั้งใจให้จับ
3. assertion ใหม่ต้องพิสูจน์ได้ว่า **FAIL ได้จริง** ไม่ใช่แค่ผ่าน
4. `npm run check` ต้องรันทุกครั้งก่อนรายงานผลงาน
5. **ทุก endpoint ที่มี response model ต้องมีเทสต์ยิงฐานข้อมูลจริง ที่วนตรวจทุกฟิลด์ที่ model ประกาศ** (D36)
   เทสต์หน่วยที่ใช้แถวเขียนมือ **ห้ามนับเป็นหลักฐาน** ว่า SQL ตรงกับ schema ·
   endpoint ใหม่ต้องเพิ่มเข้า `tests/integration/test_list_endpoints_db.py` ·
   🔴 ห้ามแก้ด้วยการทำฟิลด์เป็น optional เพื่อให้เทสต์ผ่าน
6. `draft-queue.check.mjs` ห้ามแก้ — output ต้องเท่าเดิมทุกไบต์

**คุณภาพโค้ด**

| รายการ | ผล |
|---|---|
| `npm run lint` | **0 errors** (เหลือ 14 warnings) |
| `npx tsc --noEmit` | สะอาด |
| `npm run build` | ผ่าน |

14 warnings ที่เหลือ: `no-unused-vars` 12 ตัว และ `exhaustive-deps` 2 ตัว
กระจายอยู่ใน 9 ไฟล์ — ไม่ใช่ error และยังไม่ได้แตะ **รอคำสั่งว่าจะเก็บหรือปล่อย**

### 2.4 ของที่ยังไม่ได้ทำ

| เรื่อง | สถานะ |
|---|---|
| **การทดสอบด้วยมือ T1–T10** | ยังไม่ได้ทำ — คือส่วนที่ 1 ของเอกสารนี้ |
| **งาน 5.8ก จัดการผู้ใช้** | 🛑 ติดรออนุมัติ **A-1** เพราะต้องใช้ service_role key |
| แอปมือถือ | เฟสถัดไป ตาม D13 |

**งานค้างหลัง Gate (ไม่บล็อก Gate)**

| เรื่อง | รายละเอียด |
|---|---|
| 14 lint warnings | `no-unused-vars` 12 · `exhaustive-deps` 2 · กระจายใน 9 ไฟล์ · ไม่ใช่ error |
| `/receiving/[id]` โหลดร่างโดยไม่เช็คสิทธิ์ก่อน | หน้าอื่นใช้ `enabled: allowed` แต่หน้านี้โหลดใน effect ธรรมดา · staff ที่พิมพ์ URL จะยิง 1 คำขอที่ backend ปฏิเสธ (403) · ไม่ใช่ข้อมูลรั่ว แต่ไม่สม่ำเสมอ |
| เทสต์หน่วยใช้แถวที่เขียนด้วยมือ | `MatchCursor` คืนแถวตามที่คนเขียนเทสต์พิมพ์ไว้ ไม่ใช่สิ่งที่ SQL จริง SELECT · จึงจับความไม่ตรงกันระหว่าง schema กับ SQL ไม่ได้เลย · ปิดช่องแล้วด้วย `test_list_endpoints_db.py` แต่หลักการยังอยู่ |
| ตาราง `stores` + ผูก FK ให้ทุก `store_id` พร้อมกัน | ตอนทำกฎ 65 (หลายร้าน) · ตอนนี้ `store_id` มี 6 ตาราง (medicines · purchases · sales · suppliers · store_maps · invoice_scans) เป็น uuid ธรรมดาไม่มี FK ทุกตาราง เพราะยังไม่มีตาราง stores ให้ชี้ · ทำพร้อมกันทีเดียว ไม่ทำทีละตาราง (Chat A: การทำตารางใหม่ให้ “ถูกกว่า” ตารางเก่า คือหนี้ที่แพงกว่า) |
| กฎสถานะของ `invoice_scans` 2 ข้อ | อนุมัติแล้ว ทำตอน 6.3 เป็น migration แยก: (1) `converted` ต้องมี `purchase_id` และ `purchase_id` มีได้เฉพาะตอน `converted` · (2) `failed` ต้องมี `failed_reason` · รอให้รู้ลำดับสถานะจริงของ 6.3 ก่อน |
| คอลัมน์ `thinking_tokens int null` ใน `invoice_scans` | 6.3 migration เดียวกับกฎสถานะ · แยกจาก `tokens_out` · null = ไม่ได้รายงาน · 0 = ไม่ได้คิด |
| ยืนยันอัตราคิดเงินของ token การคิด | หลังสแกนใบจริงประมาณ 10 ใบ ดูจากหน้าการใช้งานของบัญชี · ตอนนี้ใช้สมมติฐานว่าเท่าขาออก |
| ค่าใช้จ่ายต่อใบจริง | วัดตอน 6.3 จากใบจริงใบแรก (ตัวเลขประมาณของ Chat A: 3.5-flash-lite ราว 0.2 บาท · 3.6-flash ราว 1.1 บาท — **เป็นการประมาณ ไม่ใช่ค่าวัด**) · ใช้ตัดสินตอน 6.7 |
| ไฟล์ check 5 ไฟล์ยัง slice ระหว่าง marker (ผิด D43-c) | `adjust-409` (4 จุด) · `auth-profile` (2) · `menu-groups` (1) · `sell-shortcuts` (1) · `stock-detail-error` (1) · (`dumbbell-chart` เก็บไปแล้วใน `7fd0edd` เพราะถูกเขียนใหม่ทั้งไฟล์ตาม D45 อยู่แล้ว) · สแกนเมื่อ 23 ก.ย. 2569 แล้ว **marker ทุกตัวยังตรงกับซอร์สจริง = ยังไม่มีไฟล์ไหนพังเงียบ** จึงเป็นหนี้ที่ยังไม่ครบกำหนด ไม่ใช่ไฟไหม้ · Chat A อนุมัติให้เก็บกวาด **หลัง** Line และ Scatter · วิธีแก้: `match` element ทั้งก้อน + assert ว่าหาเจอ แล้วพิสูจน์ด้วย mutation “ลบ/เปลี่ยนชื่อสิ่งที่ match” ต้อง FAIL ทุกไฟล์ · 🔴 ระหว่างนี้ **ไฟล์ check ใหม่ทุกไฟล์ต้องทำตาม D43-c ตั้งแต่แรก ห้ามเพิ่มหนี้ก้อนใหม่** |

**D32 — ตัดสินแล้ว (ทางเลือก ข.)**

สิทธิ์ปัจจุบันถูกต้องอยู่แล้ว: staff ไม่ควรเห็นประวัติรับสินค้าและต้นทุน
โค้ดซ่อนคอลัมน์ที่ไม่เคยทำงานถูกลบออกแล้วจากทั้งหน้ารายการและหน้ารายละเอียด
และ D32 ในทะเบียนคำวินิจฉัยแก้เป็น “จำกัดที่ระดับหน้า ด้วย `viewReports`”
การป้องกันย้ายไปอยู่ที่ `history-access.check.mjs` ซึ่งอ่านตารางสิทธิ์จริง
ไม่ได้เขียนค่าซ้ำ — ถ้าวันหนึ่งมีคนเพิ่ม staff เข้า `viewReports` เทสต์จะ FAIL ทันที

**D33 — เก็บโค้ดตายครบทุกหน้าแล้ว**

หน้าที่กั้นด้วยด่านทุกหน้าในแอป ใช้สิทธิ์ที่เป็นสับเซ็ตของ `viewCost` ทั้งหมด
ชั้นสองที่เช็ค `viewCost` ข้างในจึงเป็นจริงเสมอ = โค้ดตาย ลบออกหมดแล้วจาก

| หน้า | ที่ลบ |
|---|---|
| ภาพรวมร้าน | `canSeeValue` + prop ของ `RiskSummaryCards` |
| ใกล้หมดอายุ | `canSeeValue` + prop ของ `ExpiringTable` |
| หมดอายุแล้ว | `canSeeValue` (คอลัมน์มูลค่า + กล่องสรุปความสูญเสีย) |
| ประวัติการขาย รายการ | `canSeeValue` (คอลัมน์ยอดรวม) |
| ประวัติการขาย รายละเอียด | `canSeeValue` (คอลัมน์ราคา/รวม + ยอดรวม) |
| ประวัติรับสินค้า ทั้งสองหน้า | `canSeeCost` (ลบไปแล้วใน D32) |

> 🔴 **ห้ามลบของ 3 หน้านี้** — `/stock`, `/stock/[id]`, `/sell`
> ทั้งสามไม่มีด่านกั้นหน้า staff เปิดได้จริงทุกวัน
> `canSeeValue` / `canSeeCost` / `canSeePrice` ในสามหน้านี้ **ทำงานจริง**
> ลบแล้วพนักงานขายจะเห็นต้นทุนที่ร้านจ่าย
> `history-access.check.mjs` จะ FAIL ทันทีถ้ามีคนลบ

### 2.5 งาน Phase 6 ที่ผู้ใช้สั่งไว้แล้ว (ห้ามแตะก่อนปิด Phase 5 Gate)

ผู้ใช้แจ้งบริบทสำคัญ: **คนในบ้านที่สูงอายุจะเป็นผู้ใช้ด้วย**

| # | เรื่อง | หมายเหตุ |
|---|---|---|
| U-1 | ปุ่ม **+** เก็บคำใน “รูปแบบ” และ “หมวดหมู่” ไว้เลือกครั้งถัดไป | ต้องมีที่เก็บถาวร — ตาราง lookup ใหม่ หรือ DISTINCT จาก `medicines` · **รอคำวินิจฉัย** |
| U-2 | พิมพ์แล้วขึ้นคำแนะนำจาง ๆ กดรับคำเดิมได้ | |
| U-3 | ลดความรกของเมนู (ตอนนี้ 15 เมนูเรียงแถวเดียว) | เสนอจัดกลุ่ม: ขายประจำวัน / คลังและรับของ / รายงาน / ตั้งค่า |
| U-4 | ใช้งานบนมือถือได้ | 🔴 ดูหัวข้อถัดไป — ตอนนี้ใช้ไม่ได้จริง |
| U-5 | คำค้นหา/คำที่ใช้บ่อย ในทุกหน้าที่มีช่องค้น | |
| U-6 | ช่องตัวเลขมี Wheel Picker และ Spin Button | เสนอ: มือถือใช้ Wheel · เดสก์ท็อปใช้ Spinner + พิมพ์ได้ · **รอคำวินิจฉัย** |
| U-7 | กราฟบน Dashboard เลือกชนิดได้ | **D35 — วาดด้วย inline SVG เท่านั้น ห้าม library** |
| U-8 | **ผังร้าน 2D/2.5D** — จิ้มจุดแทนการพิมพ์ชื่อยา | **ตัวเลือกเสริม ไม่ใช่ระบบหลัก** · 8 งานย่อย · 7 ใน 8 ไม่แตะระบบหลักเลย · ดูข้อกำหนด U-8.4 ด้านล่าง |

**🔴 U-4 — บนมือถือตอนนี้ไปหน้าอื่นไม่ได้เลย**

วัดที่ขนาด 375px ด้วยโค้ด: **ลิงก์เมนูที่มองเห็นได้ = 0 อัน**
`layout.tsx` ซ่อน `<aside>` ด้วย `hidden sm:block` แต่ไม่มีอะไรมาแทน
ผู้ใช้เปิดหน้าไหนก็ติดอยู่หน้านั้น เห็นแค่ปุ่ม “ออกจากระบบ” และชื่อร้านก็หายไปด้วย
ไม่ใช่เรื่องความสวยงาม แต่คือใช้งานไม่ได้ — เสนอให้ทำก่อนข้ออื่นใน Phase 6
### U-8 ผังร้าน — งานย่อยและข้อกำหนด

> **ผังเป็นอีกวิธีกรอกที่ผู้ใช้จะใช้หรือไม่ใช้ก็ได้**
> ระบบหลัก สแกน → เข้าคลัง → ขายยา (พิมพ์/ยิงบาร์โค้ด) ห้ามแตะ ห้ามเปลี่ยน ห้ามบังคับให้ผ่านผัง
> ผังพังเมื่อไร ระบบหลักต้องทำงานได้ครบทุกอย่าง
> `store-map-page.check.mjs` มี assertion ที่ **ห้ามลบเด็ดขาด**: ถ้าหน้าขาย หน้ารับสินค้า
> หรือหน้าคลังยา เริ่ม `import` ผังเมื่อไร check จะ FAIL ทันที

| งาน | สถานะ | แตะระบบหลัก |
|---|---|---|
| U-8.1 migration 009 + 4 ตาราง | ✅ `29f4930` | ไม่ |
| U-8.2 endpoint 8 ตัว + สิทธิ์ + เทสต์ | ✅ `2b080b1` | ไม่ |
| U-8.3 หน้าแสดงผล 2D อ่านอย่างเดียว | ✅ `2e59f80` | ไม่ |
| U-8.4 โหมดแก้ไข: จิ้มเพิ่มจุด ตั้งชื่อ ลาก ลบ **ซูม/เลื่อน** | ยังไม่เริ่ม | ไม่ |
| U-8.5 ผูกยาเข้ากับจุด | ยังไม่เริ่ม | ไม่ |
| U-8.6 สลับมุมมอง 2.5D | ยังไม่เริ่ม | ไม่ |
| U-8.7 ตัวกรอง ล็อต/วันหมดอายุ/วันที่/บริษัท | ยังไม่เริ่ม | ไม่ |
| U-8.8 ปุ่ม “เลือกจากผัง” ในหน้ารับเข้าและหน้าขาย | 🛑 **ต้องขออนุมัติแยกก่อนลงมือ** | **ใช่ — จุดเดียว** |

**🔴 ข้อกำหนดเพิ่มสำหรับ U-8.4 — จุดที่ซ้อนกัน**

วัดจากของจริงแล้ว (D46): ผัง 1000 จุดบนจอ 375px กลายเป็นก้อนทึบ กลบห้องและชั้นวางจนหมด
ตรงกับเลขที่คำนวณไว้ล่วงหน้าเป๊ะ — พื้นที่ห้องใส่เป้านิ้วได้ 65 เป้า ไม่ใช่ 1000

1. **ต้องมีตัวบอกจำนวนจุดที่ซ้อนกันในบริเวณนั้น** ห้ามปล่อยให้หมุดทับกันเฉย ๆ
2. **ผู้ใช้ต้องรู้ได้ว่า “ตรงนี้มีหลายจุด” โดยไม่ต้องซูมเข้าไปดูก่อน**
3. ตัวเลขจำนวนที่ซ้อนกันเป็นข้อความ ไม่ใช่ขนาดหรือสีของหมุดอย่างเดียว (D34)
4. ตัวเลขทั้งหมดที่แสดง ต้องมาจาก API ห้ามรวมเองในเบราว์เซอร์

**ตัวเลขความเร็วที่วัดไว้แล้ว (ผัง 1000 จุด)**

| | 375px | 1280px |
|---|---|---|
| API (warm, 5 ครั้ง) | 413–496 ms · payload 196 KB | เท่ากัน |
| layout เฉลี่ย / แย่สุด | 3.63 / 4.90 ms | 1.66 ms |
| scroll เฉลี่ย / แย่สุด | 2.76 / 6.30 ms | — |
| DOM nodes | 6,276 | 6,276 |

ทุกตัวต่ำกว่าหนึ่งเฟรม (16 ms) มาก — ตัวที่กินเวลาคือ API call ไม่ใช่การวาด

> ⚠️ ตัวเลขชุดนี้วัดจากเบราว์เซอร์บน PC ที่จำลองความกว้าง 375px **ไม่ใช่ CPU มือถือจริง**
> มือถือกลาง ๆ น่าจะช้ากว่า 3–6 เท่า (layout ~15–25 ms ซึ่งยังรับได้)
> แต่ยังพูดไม่ได้ว่า “เร็วพอบนมือถือจริง” จนกว่าจะวัดบนเครื่องจริง (D41)

**U-7 — กราฟ 3 แบบ ตามที่ Chat A กำหนด**

| แบบ | แสดงอะไร | ข้อมูล |
|---|---|---|
| Line Chart | ยอดขายรายวัน ย้อนหลัง 30 วัน | 🔴 ต้องทำ endpoint ใหม่ `GET /reports/sales-timeseries` · group by business date Asia/Bangkok ตาม **D9** ห้ามใช้ `CURRENT_DATE` |
| Scatter Plot | ยาแต่ละตัว · X = จำนวนที่ขายได้ · Y = มูลค่าที่จม | มุมขวาบน = ขายน้อยแต่เงินจมเยอะ คือสิ่งที่เจ้าของร้านต้องเห็น (Problem 2 · Problem 3) |
| Dumbbell Chart | เทียบ “ตามใบส่งของ” กับ “รับจริง” ของแต่ละใบรับ | ใช้ข้อมูลที่มีอยู่แล้ว ไม่ต้องทำ endpoint ใหม่ → **ทำตัวนี้ได้ก่อน** |

ข้อกำหนดร่วมของกราฟ

- ผู้ใช้กดเลือกชนิดกราฟได้ · ค่าเริ่มต้นต้องเลือกตัวที่มีข้อมูลจริงเสมอ
- ข้อมูลไม่พอ → **ขึ้นข้อความบอกตรง ๆ ห้ามวาดกราฟว่างหรือเส้นสมมติ**
- ตัวเลขทุกตัวมาจาก API — ห้ามคำนวณยอดรวมเองในเบราว์เซอร์ (กฎเดิม)
- ตัวอักษรและจุดสัมผัสต้องใหญ่พอสำหรับผู้สูงอายุ และใช้บนมือถือได้

**ยังรอคำวินิจฉัยจาก Chat A**

- **U-1** เก็บคำถาวรด้วยวิธีไหน — ตาราง lookup ใหม่ (ต้อง migration) หรือ `DISTINCT` จาก `medicines` (ไม่ต้อง migration)
- **U-5** เก็บคำที่ใช้บ่อยไว้ที่ไหน — เครื่องผู้ใช้ (`localStorage`, งานหน้าจออย่างเดียว) หรือฐานข้อมูล (ต้อง migration + endpoint)
- **U-6** Wheel กับ Spinner แยกตามอุปกรณ์หรือไม่

> **D34 อนุมัติแล้ว** (23 ก.ย. 2569) บันทึกในทะเบียนคำวินิจฉัยเรียบร้อย —
> ผู้ใช้หลักเป็นผู้สูงอายุและใช้งานบนมือถือ · ตัวอักษรและพื้นที่กดต้องใหญ่พอ ·
> ห้ามใช้สีอย่างเดียวสื่อความหมาย · ทุกหน้าใหม่ต้องใช้ได้บนมือถือ · ใช้กับทุกเฟส

### 2.6 เรื่องความปลอดภัยที่ยืนยันแล้ว

- ไม่มี `.env`, `.env.test`, `web/.env.local`, `.venv`, `node_modules` ถูก commit เลยตลอดเฟส
- ไม่มีรหัสผ่าน / API key / connection string อยู่ในไฟล์ migration หรือไฟล์ใดที่ commit
- ไม่มีข้อมูลร้านจริงอยู่ใน migration หรือใน code — ผู้ใช้กรอกเองผ่านหน้าจอ
- integration test ต่อเฉพาะโปรเจกต์ test ตรวจโดย `tests/db_guard.py`
- เซิร์ฟเวอร์ที่เปิดอยู่ตอนนี้อ่านค่าจาก `backend/.env` = **ฐานข้อมูล prod**
  (ยืนยันโดยเทียบว่า project ref ของ `.env` กับ `.env.test` เป็นคนละตัว โดยไม่แสดงค่าใด ๆ)
